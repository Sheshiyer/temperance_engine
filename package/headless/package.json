{
  "name": "@thoughtseed/temperance-headless-shadow",
  "version": "0.1.0-shadow.1",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "standalone:audit": "node test/standalone-audit.mjs",
    "standalone:smoke": "node test/standalone-smoke.mjs",
    "gates": "npm test && npm run standalone:audit && npm run standalone:smoke"
  }
}
