---
phase: 01-provenance-contract-and-read-only-control-plane
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package/install-surface/package.json
  - package/install-surface/bun.lock
  - package/install-surface/schemas/fragment.v1.schema.json
  - package/install-surface/schemas/lock.v1.schema.json
  - package/install-surface/schemas/private-registry.v1.schema.json
  - package/install-surface/schemas/doctor-report.v1.schema.json
  - package/install-surface/src/types.ts
  - package/install-surface/src/canonical-json.ts
  - package/install-surface/src/schema.ts
  - package/install-surface/test/fixtures/schema/valid-fragment.v1.json
  - package/install-surface/test/fixtures/schema/unknown-field.json
  - package/install-surface/test/fixtures/schema/unsupported-version.json
  - package/install-surface/test/schema.test.ts
autonomous: true
requirements:
  - PROV-01
must_haves:
  truths:
    - "Ajv strict Draft 2020-12 validation rejects unknown fields and unsupported versions before any semantic or filesystem work"
    - "Supported fragment/lock/registry/report schemas accept a valid v1.0 fixture"
    - "package/install-surface is a standalone ESM package whose only new production dependency is ajv@8.20.0"
  artifacts:
    - package/install-surface/package.json
    - package/install-surface/bun.lock
    - package/install-surface/src/schema.ts
    - package/install-surface/test/schema.test.ts
  key_links:
    - "schema.ts compiles schemas/*.v1.schema.json and is the only structural validator"
---

<objective>
Create the standalone `package/install-surface` package, strict v1 JSON schemas, typed schema literals, canonical JSON helpers, and Wave 0 schema tests that prove PROV-01 (validate before any lifecycle or semantic work).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-CONTEXT.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-RESEARCH.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-PATTERNS.md
@package/manifest-bridge/package.json
@package/headless/schema/shadow-attempt.v1.schema.json
@package/router/omniroute-cloudflare-production-adapter.ts
</context>

<threat_model>
Trust boundaries: authored JSON fragments (untrusted input) → schema compiler (trusted) → no filesystem mutation in this plan.

| threat_id | category | component | disposition | mitigation_plan |
|---|---|---|---|---|
| T-01 | Tampering | schema.ts | mitigate | Ajv `strict: true`, `allErrors: true`, `additionalProperties: false` / `unevaluatedProperties: false`; unknown fields fail before semantics |
| T-05 | Denial of service | schema.ts | mitigate | Named byte/record/path bounds constants; oversized fixtures fail closed |
| T-07 | Tampering | lock schema | mitigate | Lock schema forbids `generated_at`, absolute paths, and self-digest fields |
</threat_model>

<tasks>

<task type="tdd">
  <name>Scaffold package.json and pin ajv@8.20.0</name>
  <files>package/install-surface/package.json, package/install-surface/bun.lock</files>
  <read_first>package/manifest-bridge/package.json, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-RESEARCH.md, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-PATTERNS.md</read_first>
  <action>Create `package/install-surface/package.json` with name `temperance-install-surface`, `"private": true`, `"type": "module"`, scripts `test` = `bun test` and `doctor` = `bun run src/cli.ts doctor`. Run `cd package/install-surface && bun add -E --ignore-scripts ajv@8.20.0`. Do not add pg, jq, or Manifest Bridge as dependencies. Do not create `src/cli.ts` yet beyond leaving the script string in package.json.</action>
  <verify>
    <automated>cd package/install-surface && python3 -c "import json; d=json.load(open('package.json')); assert d['dependencies']['ajv']=='8.20.0'" && test -f bun.lock</automated>
  </verify>
  <acceptance_criteria>
    - package/install-surface/package.json contains `"name": "temperance-install-surface"`
    - package/install-surface/package.json contains `"ajv": "8.20.0"`
    - package/install-surface/package.json does not contain `pg`
    - package/install-surface/bun.lock exists
  </acceptance_criteria>
  <done>Isolated package with exact Ajv pin and lockfile</done>
</task>

<task type="tdd">
  <name>Write failing schema.test.ts fixtures</name>
  <files>package/install-surface/test/schema.test.ts, package/install-surface/test/fixtures/schema/valid-fragment.v1.json, package/install-surface/test/fixtures/schema/unknown-field.json, package/install-surface/test/fixtures/schema/unsupported-version.json</files>
  <read_first>package/headless/test/runtime.test.mjs, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-VALIDATION.md, package/install-surface/package.json</read_first>
  <action>Add bun:test file `test/schema.test.ts` that imports `validateFragment` / `validateLock` from `../src/schema.ts`. Cases: valid v1.0 fragment passes; unknown nested field fails; missing required field fails; unsupported `{major:2}` and unknown minor fail; oversized input above the named byte bound fails. Fixtures live under `test/fixtures/schema/`. Tests must fail until schema.ts exists (RED).</action>
  <verify>
    <automated>cd package/install-surface && bun test test/schema.test.ts ; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - test/schema.test.ts contains `unknown-field` or `unknown field`
    - test/schema.test.ts contains `unsupported`
    - test/fixtures/schema/valid-fragment.v1.json exists
    - bun test test/schema.test.ts is RED before schema.ts is complete
  </acceptance_criteria>
  <done>Wave 0 schema tests exist and fail for the right reasons</done>
</task>

<task type="auto">
  <name>Author Draft 2020-12 schemas</name>
  <files>package/install-surface/schemas/fragment.v1.schema.json, package/install-surface/schemas/lock.v1.schema.json, package/install-surface/schemas/private-registry.v1.schema.json, package/install-surface/schemas/doctor-report.v1.schema.json</files>
  <read_first>package/headless/schema/shadow-attempt.v1.schema.json, package/router/omniroute-promotion.schema.json, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-RESEARCH.md</read_first>
  <action>Write four JSON Schema Draft 2020-12 documents. Use `$schema` draft/2020-12. Put `additionalProperties: false` on leaf objects and `unevaluatedProperties: false` on composed unions. Version field is `{major, minor}` plus a stable major URI. Fragment records require immutable semantic `id`, `owner`, `class` enum COPY|TRANSFORM|REGENERATE|NEVER-SHIP, repository-relative `source` where applicable, destination `{root_token, relative_path, ownership}`, authority references (requirement IDs + ISA citation), platform/profile eligibility, verification method, rollback policy, and optional `identity_migration` (`from_id`, `to_id`) so D-03 source moves can keep a stable id. Lock schema is the compiled inventory only: sorted records, no `generated_at`, no absolute paths, no self-digest. Private-registry schema is host-only (id, class, enabled, binding, label, provider, notes, policy_rule). Doctor-report schema is `temperance.doctor.report.v1` with overall_condition enum PASS|DRIFT|WARN|FAIL|SKIPPED|UNSUPPORTED|PRIVATE|UNAVAILABLE, exit_code 0|1|2, manifest_digest `sha256:` string, and check records with id, source, destination, class, expected_state, actual_state, condition, reason_code, severity, actionable, remediation, evidence.</action>
  <verify>
    <automated>python3 -c "import json,pathlib; [json.load(open(p)) for p in pathlib.Path('package/install-surface/schemas').glob('*.json')]"</automated>
  </verify>
  <acceptance_criteria>
    - schemas/fragment.v1.schema.json contains `"additionalProperties": false`
    - schemas/lock.v1.schema.json does not contain generated_at as a required property
    - schemas/doctor-report.v1.schema.json contains `temperance.doctor.report.v1`
    - schemas/private-registry.v1.schema.json contains `binding`
  </acceptance_criteria>
  <done>Four strict v1 schemas exist</done>
</task>

<task type="auto">
  <name>Add types.ts schema literals and canonical-json.ts</name>
  <files>package/install-surface/src/types.ts, package/install-surface/src/canonical-json.ts</files>
  <read_first>package/manifest-bridge/src/types.ts, package/router/omniroute-cloudflare-production-adapter.ts, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-PATTERNS.md</read_first>
  <action>In types.ts export schema name literals as const matching PATTERNS.md: `temperance.install-surface.fragment.v1`, `temperance.install-surface.lock.v1`, `temperance.private-registry.v1`, `temperance.doctor.report.v1`. Discriminated union on `class`. Destination type `{root_token: string, relative_path: string, ownership: {kind: 'exclusive-path' | 'managed-block', marker_id?: string}}`. DoctorCondition and DoctorExitCode unions matching D-18/D-19. In canonical-json.ts copy the Cloudflare adapter `canonical()` idea: NFC-normalize strings, recursively sort object keys, sort arrays of records by semantic `id`, serialize with two-space indent and exactly one trailing newline. Do not include time, cwd, hostname, or inode.</action>
  <verify>
    <automated>rg -n "temperance.doctor.report.v1" package/install-surface/src/types.ts && rg -n "canonical" package/install-surface/src/canonical-json.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/types.ts contains `temperance.install-surface.fragment.v1`
    - src/types.ts contains `temperance.doctor.report.v1`
    - src/types.ts contains `exclusive-path`
    - src/canonical-json.ts contains a function named `canonical`
  </acceptance_criteria>
  <done>Typed literals and canonical serializer exist</done>
</task>

<task type="auto">
  <name>Implement schema.ts Ajv compiler and turn tests GREEN</name>
  <files>package/install-surface/src/schema.ts</files>
  <read_first>package/install-surface/src/types.ts, package/install-surface/schemas/fragment.v1.schema.json, package/install-surface/test/schema.test.ts, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-RESEARCH.md</read_first>
  <action>Instantiate Ajv 2020-12 export with `{strict: true, allErrors: true}`. Never set `validateSchema: false`. Export `validateFragment`, `validateLock`, `validatePrivateRegistry`, `validateDoctorReport`. Bound input bytes via a named constant (document the number in the constant name, e.g. `MAX_FRAGMENT_BYTES`). Do not import `node:fs` write APIs. Do not call semantic/path validators from this module. Run `cd package/install-surface && bun test test/schema.test.ts` until GREEN.</action>
  <verify>
    <automated>cd package/install-surface && bun test test/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/schema.ts contains `strict: true`
    - src/schema.ts contains `allErrors: true`
    - src/schema.ts does not contain `validateSchema: false`
    - `cd package/install-surface && bun test test/schema.test.ts` exits 0
  </acceptance_criteria>
  <done>PROV-01 structural validation is green</done>
</task>

</tasks>

<verification>
- [ ] `cd package/install-surface && bun test test/schema.test.ts` exits 0
- [ ] `ajv` is exactly 8.20.0 in package.json
- [ ] No `generated_at` required on lock schema
- [ ] package does not depend on `pg`
</verification>

<success_criteria>
- PROV-01: unsupported versions and unknown fields fail before semantic work
- Standalone package exists with bun.lock
- Wave 0 schema tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-provenance-contract-and-read-only-control-plane/01-01-SUMMARY.md`
</output>
