---
phase: 01-provenance-contract-and-read-only-control-plane
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - package/install-surface/src/private-registry.ts
  - package/install-surface/src/doctor/model.ts
  - package/install-surface/src/doctor/orchestrator.ts
  - package/install-surface/src/doctor/render-human.ts
  - package/install-surface/src/doctor/render-json.ts
  - package/install-surface/src/doctor/sections/install.ts
  - package/install-surface/src/doctor/sections/privacy.ts
  - package/install-surface/src/doctor/sections/runtime.ts
  - package/install-surface/src/cli.ts
  - bin/temperance
  - scripts/temperance-doctor.sh
  - tests/temperance-doctor.sh
  - scripts/verify-all.sh
  - package/manifest-bridge/src/cli.ts
  - package/manifest-bridge/src/doctor.ts
  - package/install-surface/test/doctor.test.ts
  - package/install-surface/test/cli.test.ts
autonomous: true
requirements:
  - DOCT-01
  - DOCT-02
  - DOCT-03
  - DOCT-04
  - DOCT-05
  - SAFE-07
must_haves:
  truths:
    - "temperance doctor is permanently read-only and shares one report object between human and JSON renderers"
    - "Drift yields condition DRIFT and exit 1 without changing fixture bytes, modes, or timestamps"
    - "Required FAIL, optional SKIPPED, platform UNSUPPORTED, overlay PRIVATE, and section UNAVAILABLE remain distinct"
    - "COPY, TRANSFORM, REGENERATE, and NEVER-SHIP use different verification rules"
    - "Mutating flags --record and --repair-duplicates are rejected on the public doctor surface with migration text"
  artifacts:
    - bin/temperance
    - package/install-surface/src/doctor/orchestrator.ts
    - package/install-surface/test/doctor.test.ts
    - package/install-surface/test/cli.test.ts
  key_links:
    - "cli.ts doctor subcommand → orchestrator → section adapters → single DoctorReportV1 → render-human or render-json"
---

<objective>
Ship the public read-only `temperance doctor` composer (install, privacy, runtime), human/JSON renderers, compatibility wrappers, class-aware verification, and doctor tests covering DOCT-01..05 and SAFE-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-CONTEXT.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-RESEARCH.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-PATTERNS.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-02-PLAN.md
@package/manifest-bridge/src/doctor.ts
@package/manifest-bridge/src/cli.ts
@package/manifest-bridge/src/runtime-status.ts
@scripts/temperance-doctor.sh
@tests/temperance-doctor.sh
@scripts/verify-all.sh
</context>

<threat_model>
Trust boundaries: committed lockfile (reviewed) + host private registry (untrusted) + runtime probes (untrusted) → ObservationIO (read-only) → DoctorReportV1 → stdout. No mutation callback crosses the boundary.

| threat_id | category | component | disposition | mitigation_plan |
|---|---|---|---|---|
| T-06 | Information disclosure | privacy.ts / render-* | mitigate | Explicit allowlist projection (schema_version, id, class, enabled, presence, policy_rule). Honeytoken tests on human, JSON, verbose, stderr |
| T-08 | Elevation of privilege | orchestrator.ts | mitigate | ObservationIO has readFile/lstat/realpath/bounded fetch/allowlisted execFile/now only. No writeFile, no pg Pool, no repair |
| T-03 | Tampering | install.ts | mitigate | Class-aware checksums; NEVER-SHIP never digests or traverses |
| T-05 | Denial of service | orchestrator.ts | mitigate | Per-section timeouts (install 2000ms, privacy 750ms, runtime 4000ms) with AbortSignal; UNAVAILABLE isolation |
</threat_model>

<tasks>

<task type="auto">
  <name>Define ObservationIO and doctor model</name>
  <files>package/install-surface/src/doctor/model.ts, package/install-surface/src/private-registry.ts</files>
  <read_first>package/manifest-bridge/src/doctor.ts, package/router/omniroute-cloudflare-production-adapter.ts, package/install-surface/src/types.ts</read_first>
  <action>Export `ObservationIO` with only `readFile`, `lstat`, `realpath`, bounded `fetch`, allowlisted `execFile`, and `now`. Export `DoctorReportV1` matching RESEARCH Pattern 7 (`schema: "temperance.doctor.report.v1"`, version `{major:1,minor:0}`, generated_at, scope, trustworthy, overall_condition, exit_code, manifest_digest, sections). Every check includes id, source, destination, class, expected_state, actual_state, condition, reason_code, severity, actionable, remediation, evidence. `private-registry.ts` reads `<operator Temperance state root>/private-overlays/registry.v1.json` with `openOwnerOnlyRegular` rules (mode 0600, parent 0700, nlink 1, no symlink). Absent file = empty registry + SKIPPED without creating files. Public projection is explicit field pick, never `{...private, binding: redacted}`. Do not implement overlay register/unregister mutation (D-12 deferred to Phase 3); observation never traverses, moves, modifies, or deletes overlay data.</action>
  <verify>
    <automated>rg -n "ObservationIO" package/install-surface/src/doctor/model.ts && rg -n "writeFile" package/install-surface/src/doctor/model.ts ; test $? -ne 0 || ! rg -n "writeFile" package/install-surface/src/doctor/model.ts</automated>
  </verify>
  <acceptance_criteria>
    - doctor/model.ts contains `ObservationIO`
    - doctor/model.ts contains `temperance.doctor.report.v1`
    - doctor/model.ts does not contain `writeFile`
    - private-registry.ts contains `SKIPPED`
  </acceptance_criteria>
  <done>Read-only doctor types exist</done>
</task>

<task type="auto">
  <name>Implement section adapters and orchestrator</name>
  <files>package/install-surface/src/doctor/orchestrator.ts, package/install-surface/src/doctor/sections/install.ts, package/install-surface/src/doctor/sections/privacy.ts, package/install-surface/src/doctor/sections/runtime.ts</files>
  <read_first>package/install-surface/src/load.ts, package/manifest-bridge/src/runtime-status.ts, package/manifest-bridge/src/doctor.ts, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-RESEARCH.md</read_first>
  <action>Install section loads committed lock bytes via `loadLock` (never compileFragments), observes destinations with class-aware rules: COPY byte digest, TRANSFORM in-memory adapter output digest, REGENERATE semantic probe, NEVER-SHIP symbolic/presence-only (no path, no digest). Privacy section uses public projection + D-11 ladder (SKIPPED/PRIVATE/WARN/FAIL). Runtime section wraps Manifest Bridge as a typed adapter (`manifestRuntimeReceipt` or equivalent) without parsing human text; Linux launchd probes return UNSUPPORTED and must not invoke launchctl. Orchestrator owns timeouts, exception→UNAVAILABLE, canonical section order install/privacy/runtime, aggregation FAIL>DRIFT>WARN>UNAVAILABLE else PASS, exit 0/1/2 per D-19. Filtered `--section` marks `scope.complete=false`.</action>
  <verify>
    <automated>rg -n "loadLock" package/install-surface/src/doctor/sections/install.ts && rg -n "UNAVAILABLE" package/install-surface/src/doctor/orchestrator.ts</automated>
  </verify>
  <acceptance_criteria>
    - sections/install.ts contains `loadLock` or equivalent committed-byte loader
    - sections/install.ts does not contain `compileFragments`
    - sections/privacy.ts contains `PRIVATE`
    - orchestrator.ts contains `UNAVAILABLE`
    - sections/runtime.ts contains `UNSUPPORTED`
  </acceptance_criteria>
  <done>Three sections compose one trustworthy report</done>
</task>

<task type="auto">
  <name>Render human and JSON from the same object</name>
  <files>package/install-surface/src/doctor/render-human.ts, package/install-surface/src/doctor/render-json.ts, package/install-surface/src/cli.ts</files>
  <read_first>package/manifest-bridge/src/cli.ts, package/manifest-bridge/src/doctor.ts, package/install-surface/src/doctor/model.ts</read_first>
  <action>render-json emits one versioned envelope (canonical key order). render-human is drift-first: overall, section summaries, every non-healthy item, remediation first; `--verbose` expands public-safe records. `cli.ts` subcommand `doctor` supports repeatable `--section install|privacy|runtime`, `--json`, `--verbose`. Invalid section → exit 2. Do not implement `--record` or `--repair-duplicates` on this CLI. Default (no filters) runs all sections.</action>
  <verify>
    <automated>rg -n "doctor" package/install-surface/src/cli.ts && rg -n "repair" package/install-surface/src/cli.ts ; test $? -ne 0 || ! rg -n "repair-duplicates" package/install-surface/src/cli.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/cli.ts contains `--section`
    - src/cli.ts contains `--json`
    - src/cli.ts does not contain `repair-duplicates`
    - render-human.ts contains `remediation`
  </acceptance_criteria>
  <done>DOCT-01..03 presentation surface exists</done>
</task>

<task type="auto">
  <name>Add public wrapper and migrate legacy doctor</name>
  <files>bin/temperance, scripts/temperance-doctor.sh, tests/temperance-doctor.sh, scripts/verify-all.sh, package/manifest-bridge/src/cli.ts, package/manifest-bridge/src/doctor.ts</files>
  <read_first>package/headless/bin/temperance-shadow, scripts/temperance-doctor.sh, tests/temperance-doctor.sh, scripts/verify-all.sh, package/manifest-bridge/src/cli.ts, package/manifest-bridge/src/doctor.ts</read_first>
  <action>Add `bin/temperance` POSIX wrapper that execs `bun run src/cli.ts` from package/install-surface. Rewrite `scripts/temperance-doctor.sh` as a thin caller of `bin/temperance doctor` that does not require jq. If argv contains `--record` or `--repair-duplicates`, exit 2 with migration text pointing at future Phase 3 repair commands (do not repair). Keep Manifest Bridge `doctor` as a component adapter for the runtime section; strip `--record`/`--repair-duplicates` from the public usage string and reject those flags with the same migration text. In `package/manifest-bridge/src/doctor.ts`, delete the doctor-path `options.record` `writeFileSync` diagnostics block and stop invoking `repairDuplicateEvents` from `runManifestDoctor` (D-14: every doctor command is read-only). Leave `repairDuplicateEvents` in-tree only if a non-doctor command calls it. Extend `tests/temperance-doctor.sh` and `scripts/verify-all.sh` to run the public entrypoint plus a lock-drift gate (`compile` without write vs committed lock bytes). Do not suppress the existing Phase 2 verify.sh private-path baseline.</action>
  <verify>
    <automated>rg -n "bin/temperance" scripts/temperance-doctor.sh && rg -n "repair-duplicates" scripts/temperance-doctor.sh</automated>
  </verify>
  <acceptance_criteria>
    - bin/temperance exists and is executable or documented as bun-invoked
    - scripts/temperance-doctor.sh contains `doctor`
    - scripts/temperance-doctor.sh rejects or does not implement repair
    - scripts/verify-all.sh contains `install-surface` or `temperance doctor`
    - package/manifest-bridge/src/doctor.ts `runManifestDoctor` does not call `writeFileSync` or `repairDuplicateEvents`
  </acceptance_criteria>
  <done>Public doctor entrypoint is the typed CLI</done>
</task>

<task type="tdd">
  <name>Doctor and CLI tests including read-only invariant</name>
  <files>package/install-surface/test/doctor.test.ts, package/install-surface/test/cli.test.ts</files>
  <read_first>.planning/phases/01-provenance-contract-and-read-only-control-plane/01-VALIDATION.md, package/manifest-bridge/test/bridge.test.ts, tests/temperance-doctor.sh</read_first>
  <action>doctor.test.ts: record-contract (every check has the common field set); read-only invariant (snapshot entries/bytes/modes/links/mtime before and after doctor; ObservationIO fake has no write methods); eligibility table FAIL vs SKIPPED vs UNSUPPORTED; class-aware COPY/TRANSFORM/REGENERATE/NEVER-SHIP; timeout isolation yields UNAVAILABLE + exit 1. cli.test.ts: human order/remediation; drift → DRIFT + exit 1; JSON/human share observations; CLI matrix default/filtered/verbose/json and exits 0/1/2. Privacy: registry mode 0644, symlink, and nlink!=1 fail closed; honeytokens in binding/label/provider/notes/error paths are absent from human, JSON, verbose, and stderr. No watch flags.</action>
  <verify>
    <automated>cd package/install-surface && bun test test/doctor.test.ts test/cli.test.ts && cd ../.. && bash tests/temperance-doctor.sh</automated>
  </verify>
  <acceptance_criteria>
    - test/doctor.test.ts contains `read-only` or `mtime`
    - test/doctor.test.ts contains `UNSUPPORTED` or `eligibility`
    - test/cli.test.ts contains `DRIFT`
    - test/doctor.test.ts or test/cli.test.ts contains a honeytoken assertion
    - bun test doctor+cli exits 0
  </acceptance_criteria>
  <done>DOCT-01..05 and SAFE-07 tests green</done>
</task>

</tasks>

<verification>
- [ ] `cd package/install-surface && bun test` exits 0
- [ ] `bash tests/temperance-doctor.sh` exits 0 on a healthy fixture
- [ ] Doctor run against a fixture tree does not change `find -ls` / checksum snapshot
- [ ] `--repair-duplicates` on `temperance doctor` exits 2
- [ ] `./scripts/verify-all.sh` includes install-surface tests; known Phase 2 path-hygiene failure is not broadly suppressed
</verification>

<success_criteria>
- User can run doctor human or JSON and receive the same observations without repair
- Drift, required failure, optional skip, unsupported, PRIVATE, and class-aware checksums are distinguishable
- Unsafe destination resolution remains fail-closed from plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-provenance-contract-and-read-only-control-plane/01-03-SUMMARY.md`
</output>
