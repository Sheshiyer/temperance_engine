---
phase: 01-provenance-contract-and-read-only-control-plane
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - package/install-surface/src/path-policy.ts
  - package/install-surface/src/deny-policy.ts
  - package/install-surface/src/semantic-validation.ts
  - package/install-surface/src/authority.ts
  - package/install-surface/src/compile.ts
  - package/install-surface/src/load.ts
  - package/install-surface/deny-policy.v1.json
  - package/install-surface/fragments/hooks.json
  - package/install-surface/fragments/router.json
  - package/install-surface/fragments/manifest.json
  - package/install-surface/fragments/enrichment.json
  - package/install-surface/fragments/skills.json
  - package/install-surface/fragments/private-boundaries.json
  - package/install-surface/install-surface-manifest.lock.json
  - package/install-surface/src/cli.ts
  - package/install-surface/test/semantics.test.ts
  - package/install-surface/test/authority.test.ts
  - package/install-surface/test/determinism.test.ts
  - package/install-surface/test/privacy.test.ts
  - ISA.md
autonomous: false
requirements:
  - PROV-02
  - PROV-03
  - PROV-04
  - PROV-05
  - PROV-07
  - SAFE-04
must_haves:
  truths:
    - "Every included surface compiles to exactly one stable semantic ID with owner and class"
    - "Exact, ancestor, and descendant destination ownership conflicts fail closed before any lock bytes are written"
    - "Sources are repository-relative POSIX paths; destinations use allowlisted root tokens"
    - "Path escapes, dependency cycles, contradictory classes, and unsafe adapters fail closed"
    - "Fragments cannot self-ratify; ISA.md plus requirement IDs are the only scope authority"
    - "In-memory compile is byte-identical across shuffled inputs; lock write is a separate explicit command"
  artifacts:
    - package/install-surface/src/compile.ts
    - package/install-surface/install-surface-manifest.lock.json
    - package/install-surface/deny-policy.v1.json
    - package/install-surface/test/semantics.test.ts
    - package/install-surface/test/authority.test.ts
  key_links:
    - "compile.ts calls schema then semantics then authority then deny-policy, then canonical-json; writeLock is a separate export"
---

<objective>
Implement the in-memory provenance compiler, deny policy, path/ownership/authority gates, authored fragments, and the explicit lockfile write command. Bind PROV-02..05, PROV-07, and SAFE-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-CONTEXT.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-RESEARCH.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-PATTERNS.md
@.planning/phases/01-provenance-contract-and-read-only-control-plane/01-01-PLAN.md
@package/router/omniroute-native-control-plane.ts
@package/router/omniroute-cloudflare-production-adapter.ts
@package/manifest-bridge/src/catalog.ts
@ISA.md
</context>

<threat_model>
Trust boundaries: untrusted fragments → in-memory compiler (no host token bind) → explicit lock write to git-reviewed bytes. Private registry schema is validated here; live host bindings are not compiled into the lock.

| threat_id | category | component | disposition | mitigation_plan |
|---|---|---|---|---|
| T-02 | Spoofing | semantic-validation.ts | mitigate | Unique semantic IDs; segment-aware ownership trie; exclusive-path vs ancestor/descendant fail closed |
| T-03 | Tampering | path-policy.ts | mitigate | Reject absolute sources, `.` `..` `\\` NUL, unknown root tokens, non-NFC; realpath containment, not `startsWith` |
| T-04 | Elevation of privilege | semantic-validation.ts | mitigate | Adapter IDs are an allowlist enum; never a shell command string |
| T-05 | Denial of service | compile.ts | mitigate | Record/dependency/path bounds; deterministic cycle detection |
| T-06 | Information disclosure | deny-policy.ts | mitigate | Symbolic NEVER-SHIP records only; no overlay filename in fragments |
| T-07 | Tampering | compile.ts / lock write | mitigate | Compile fully in memory; writeLock only after success; CI compares exact bytes |
| T-08 | Elevation of privilege | authority.ts | mitigate | Load ISA.md + REQUIREMENTS.md; reject missing/malformed refs; no allowed-surfaces.json |
</threat_model>

<tasks>

<task type="auto">
  <name>Implement path-policy.ts</name>
  <files>package/install-surface/src/path-policy.ts</files>
  <read_first>package/router/omniroute-native-control-plane.ts, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-PATTERNS.md, package/install-surface/src/types.ts</read_first>
  <action>Export `assertRepositoryRelativeSource(path)` and `assertDestination(dest)`. Allowlisted root tokens must be a named const array including `CODEX_HOME`, `CLAUDE_CONFIG_DIR`, `HOME`, `TEMPERANCE_STATE`. Reject absolute paths, empty segments, `.`, `..`, backslashes, NUL, non-NFC, unknown tokens. Compare ownership by root token plus normalized path segments, not string prefixes. Do not resolve tokens to the host in this module.</action>
  <verify>
    <automated>rg -n "CODEX_HOME" package/install-surface/src/path-policy.ts && rg -n "startsWith" package/install-surface/src/path-policy.ts ; test $? -ne 0 || ! rg -n "startsWith" package/install-surface/src/path-policy.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/path-policy.ts contains `CODEX_HOME`
    - src/path-policy.ts contains `relative_path`
    - src/path-policy.ts does not contain `startsWith`
  </acceptance_criteria>
  <done>SAFE-04 / PROV-04 path policy exists</done>
</task>

<task type="auto">
  <name>Implement deny-policy and semantic-validation</name>
  <files>package/install-surface/src/deny-policy.ts, package/install-surface/src/semantic-validation.ts, package/install-surface/deny-policy.v1.json</files>
  <read_first>scripts/verify-install.sh, package/relocation/project-packet-schema.ts, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-RESEARCH.md</read_first>
  <action>Author `deny-policy.v1.json` with symbolic rules for secret/database/log/history/memory/backup/private-root patterns (rule IDs, not host paths). `deny-policy.ts` matches candidate repository-relative paths only. `semantic-validation.ts` enforces: unique semantic IDs; unique owner+class; exclusive-path conflicts with equal/ancestor/descendant under the same root token; managed-block requires distinct `marker_id`; dependency cycle detection; class-discriminated source/verification shapes; adapter ID allowlist (no command strings); when a prior lock exists, source-path moves keep the same semantic id and a new id that reuses an old destination/ownership identity requires an explicit `identity_migration` mapping or fail closed (D-03). Fail closed with stable reason codes `OWNERSHIP_OVERLAP`, `DEPENDENCY_CYCLE`, `ADAPTER_COMBINATION_UNSAFE`, `DENY_POLICY_MATCH`, `IDENTITY_MIGRATION_REQUIRED`.</action>
  <verify>
    <automated>rg -n "OWNERSHIP_OVERLAP" package/install-surface/src/semantic-validation.ts && rg -n "DENY_POLICY_MATCH" package/install-surface/src/deny-policy.ts</automated>
  </verify>
  <acceptance_criteria>
    - deny-policy.v1.json contains a `rules` array or equivalent rule list
    - semantic-validation.ts contains `OWNERSHIP_OVERLAP`
    - semantic-validation.ts contains `DEPENDENCY_CYCLE`
    - semantic-validation.ts contains `identity_migration` or `IDENTITY_MIGRATION_REQUIRED`
    - semantic-validation.ts does not contain `child_process`
  </acceptance_criteria>
  <done>PROV-02, PROV-03, PROV-05 semantic gates exist</done>
</task>

<task type="auto">
  <name>Implement authority.ts without a competing store</name>
  <files>package/install-surface/src/authority.ts</files>
  <read_first>ISA.md, .planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-RESEARCH.md</read_first>
  <action>Load repo `ISA.md` and `.planning/REQUIREMENTS.md` as text. Every fragment record must cite at least one Phase 1 requirement ID and an ISA ratification reference (ISC-769..ISC-788 is the existing class/workflow ratification; new semantic IDs need the checkpoint below). Reject missing, unknown, or self-declared ratification. Export `assertAuthority(records, {isaText, requirementsText})`. Do not write `allowed-surfaces.json`.</action>
  <verify>
    <automated>rg -n "ISA.md" package/install-surface/src/authority.ts && test ! -f package/install-surface/allowed-surfaces.json</automated>
  </verify>
  <acceptance_criteria>
    - src/authority.ts contains `REQUIREMENTS.md` or `requirementsText`
    - src/authority.ts contains `AUTHORITY_REFERENCE_INVALID` or equivalent reason code
    - allowed-surfaces.json does not exist
  </acceptance_criteria>
  <done>PROV-07 authority gate exists</done>
</task>

<task type="auto">
  <name>Implement in-memory compile.ts and load.ts</name>
  <files>package/install-surface/src/compile.ts, package/install-surface/src/load.ts</files>
  <read_first>package/manifest-bridge/src/catalog.ts, package/install-surface/src/schema.ts, package/install-surface/src/canonical-json.ts</read_first>
  <action>`compileFragments(inputs)` parses each fragment, runs schema → path-policy → semantics → deny-policy → authority entirely in memory, sorts records by semantic id, returns `{lockObject, canonicalBytes, digest}` where digest is `sha256:` of canonicalBytes and is NOT stored inside those bytes. `writeLock(path, canonicalBytes)` is the only writer, using same-directory temp file + rename like `persistRegistryUnlocked`. `loadLock(path)` validates committed bytes against lock schema and must not recompile from a dirty checkout. Doctor in plan 03 will call loadLock, not compileFragments.</action>
  <verify>
    <automated>rg -n "writeLock" package/install-surface/src/compile.ts && rg -n "sha256:" package/install-surface/src/compile.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/compile.ts contains `writeLock`
    - src/compile.ts contains `sha256:`
    - src/load.ts contains `validateLock` or lock schema validation
    - src/compile.ts does not write during `compileFragments`
  </acceptance_criteria>
  <done>Deterministic compiler with separate write command</done>
</task>

<task type="auto">
  <name>Author domain fragments and compile CLI write command</name>
  <files>package/install-surface/fragments/hooks.json, package/install-surface/fragments/router.json, package/install-surface/fragments/manifest.json, package/install-surface/fragments/enrichment.json, package/install-surface/fragments/skills.json, package/install-surface/fragments/private-boundaries.json, package/install-surface/src/cli.ts</files>
  <read_first>docs/plans/2026-08-19-mac-mini-to-public-temperance-glove-audit.md, package/install-surface/src/types.ts, package/headless/bin/temperance-shadow</read_first>
  <action>Author six domain fragments covering public COPY/TRANSFORM/REGENERATE surfaces named in the audit (router, hooks, Manifest Bridge/Zone, enrichment, skills) plus NEVER-SHIP private-boundary symbolic records (no `atlasRecall.ts` filename, no host path). Wire `src/cli.ts` subcommands `compile` (print digest, never write) and `write-lock` (explicit write). Do not run `write-lock` or any `--write` flag in this task; T7 writes `install-surface-manifest.lock.json` only after the ISA decision checkpoint.</action>
  <verify>
    <automated>rg -n "write-lock" package/install-surface/src/cli.ts && rg -n "NEVER-SHIP" package/install-surface/fragments/private-boundaries.json</automated>
  </verify>
  <acceptance_criteria>
    - fragments/private-boundaries.json contains `NEVER-SHIP`
    - fragments/private-boundaries.json does not contain `atlasRecall.ts`
    - src/cli.ts contains `write-lock`
    - six fragment JSON files exist under fragments/
  </acceptance_criteria>
  <done>Reviewed fragment sources exist; write-lock is explicit</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Human-ratify the first semantic-ID set</name>
  <files>ISA.md</files>
  <read_first>ISA.md, package/install-surface/src/cli.ts, package/install-surface/fragments/private-boundaries.json</read_first>
  <decision>Ratify the compiler's printed semantic-ID set into product ISA.md before the first lockfile write.</decision>
  <context>Product ISA.md currently ratifies lifecycle classes (ISC-769..788) but does not enumerate the new stable semantic IDs. PROV-07 forbids self-ratification. T7 must not write install-surface-manifest.lock.json until this set is operator-approved. Reject any ID that names a private overlay filename, host path, or unratified surface.</context>
  <options>
    <option id="approve">
      <name>Approve the printed semantic-ID set</name>
      <pros>First reviewed lockfile can be committed; CI drift gate becomes meaningful</pros>
      <cons>Those IDs become public v1.1 glove scope</cons>
    </option>
    <option id="drop">
      <name>Drop listed IDs and re-print</name>
      <pros>Keeps unratified or private-named surfaces out of the lock</pros>
      <cons>Fragments must be edited before T7</cons>
    </option>
  </options>
  <what-built>Compiler + fragment set ready to emit the first `install-surface-manifest.lock.json`. Product ISA.md currently ratifies lifecycle classes (ISC-769..788) but does not enumerate the new stable semantic IDs.</what-built>
  <how-to-verify>Review the semantic `id` list the compiler would emit (print via `bun run src/cli.ts compile` without `--write`). Confirm each ID belongs in the public v1.1 glove. Then allow the executor to append those IDs to product ISA.md. Reject any ID that names a private overlay filename, host path, or unratified surface.</how-to-verify>
  <acceptance_criteria>
    - Operator types `approved` or lists IDs to drop before write-lock
    - bun run src/cli.ts compile (no --write) prints semantic ids without writing lock bytes
  </acceptance_criteria>
  <resume-signal>Type "approved" after the semantic-ID set is acceptable, or list IDs to drop</resume-signal>
</task>

<task type="auto">
  <name>Record ratified semantic IDs in ISA.md and write lockfile</name>
  <files>ISA.md, package/install-surface/install-surface-manifest.lock.json</files>
  <read_first>ISA.md, package/install-surface/src/compile.ts</read_first>
  <action>After approval, append a dated ISA.md decision listing the ratified semantic IDs (do not create allowed-surfaces.json). Then run the explicit write-lock command and commit the exact lock bytes. Digest is reported externally, not embedded.</action>
  <verify>
    <automated>test -f package/install-surface/install-surface-manifest.lock.json && python3 -c "import json; json.load(open('package/install-surface/install-surface-manifest.lock.json'))"</automated>
  </verify>
  <acceptance_criteria>
    - ISA.md contains at least one new semantic ID string from the fragment set
    - install-surface-manifest.lock.json exists and parses as JSON
    - install-surface-manifest.lock.json does not contain `generated_at`
    - allowed-surfaces.json still does not exist
  </acceptance_criteria>
  <done>First reviewed lockfile is ISA-ratified</done>
</task>

<task type="tdd">
  <name>Add semantics, authority, determinism, and privacy tests</name>
  <files>package/install-surface/test/semantics.test.ts, package/install-surface/test/authority.test.ts, package/install-surface/test/determinism.test.ts, package/install-surface/test/privacy.test.ts</files>
  <read_first>package/headless/test/determinism.test.mjs, .planning/phases/01-provenance-contract-and-read-only-control-plane/01-VALIDATION.md, package/install-surface/src/compile.ts</read_first>
  <action>Implement VALIDATION rows: semantics tests tagged `stable`, `ownership`, `path`, `semantic`, `unsafe-paths`; authority.test.ts for PROV-07; determinism.test.ts shuffling fragment/record/dependency/object order expecting byte-identical canonicalBytes; a prior-lock fixture where a new id reuses an old destination fails without `identity_migration`; privacy.test.ts honeytokens in bindings/labels/providers/notes/errors must be absent from compile errors; deny-policy `rule-only` matches omit paths while `safe-relative-path` rules show a repository-relative path only and leave candidate bytes unchanged. bun:test. No watch mode.</action>
  <verify>
    <automated>cd package/install-surface && bun test test/schema.test.ts test/semantics.test.ts test/authority.test.ts test/determinism.test.ts test/privacy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test/semantics.test.ts contains `ownership`
    - test/determinism.test.ts compares canonical bytes or digest equality
    - test/privacy.test.ts contains a honeytoken string
    - bun test of the five files exits 0
  </acceptance_criteria>
  <done>PROV-02..05/07 and SAFE-04 tests green</done>
</task>

</tasks>

<verification>
- [ ] `cd package/install-surface && bun test test/schema.test.ts test/semantics.test.ts test/authority.test.ts test/determinism.test.ts test/privacy.test.ts` exits 0
- [ ] lockfile committed, no `generated_at`
- [ ] compile without `--write` does not change git status of the lockfile
- [ ] no `allowed-surfaces.json`
</verification>

<success_criteria>
- Maintainer can validate schema v1 and see overlaps, cycles, unsafe paths, and unratified scope rejected before mutation
- Every included surface appears exactly once with stable ID, owner, class, source, destination rule, eligibility, verification, rollback
</success_criteria>

<output>
After completion, create `.planning/phases/01-provenance-contract-and-read-only-control-plane/01-02-SUMMARY.md`
</output>
